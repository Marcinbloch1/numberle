import { Keyboard } from "../enums/keyboard.enum";

export namespace Defaults {
  export const KeyboardKeys: Keyboard[] = [
    Keyboard.One,
    Keyboard.Two,
    Keyboard.Three,
    Keyboard.Four,
    Keyboard.Five,
    Keyboard.Six,
    Keyboard.Seven,
    Keyboard.Eight,
    Keyboard.Nine,
    Keyboard.Zero,
    Keyboard.Enter,
    Keyboard.Delete,
  ];
}
